// We need this constants to store the access and the reafresh token in the local storage
// and to use them in the whole application
// local storage means the users browser here 

export const ACCESS_TOKEN = "access"
export const REFRESH_TOKEN = "refresh"
